import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    icon: {
        fontSize: 60,
        marginTop: 20,
        marginBottom: 20,
        paddingLeft: 10,
        paddingRight: 10
    },
    iconscontainer: {
        flexDirection: 'row',
    }
});