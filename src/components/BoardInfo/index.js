import React from 'react';
import { View, FlatList } from 'react-native';
import styles from './styles';

const BoardInfo = ({id, boardId, selectedImages, onLongPress}) => {
    return(
        <View>
            
        </View>
    )
}

export default BoardInfo;